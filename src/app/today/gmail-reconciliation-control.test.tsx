import {renderToStaticMarkup} from "react-dom/server";import {describe,expect,it} from "vitest";import {GmailReconciliationControl} from "./gmail-reconciliation-control";
describe("Gmail send reconciliation UI",()=>{it("explains the uncertain boundary, hides any send action, and requires explicit confirmation",()=>{const html=renderToStaticMarkup(<GmailReconciliationControl snapshotId="fixture" action={()=>{}}/>);expect(html).toContain("Resolve Send Status");expect(html).toContain("Check Gmail before choosing an outcome");expect(html).toContain("I confirmed it was sent");expect(html).toContain("I confirmed it was NOT sent");expect(html).toContain("I still cannot tell");expect(html).toContain('required=""');expect(html).not.toContain("Send Email");});});
