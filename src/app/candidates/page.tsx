import {loadDailyCommandCenter} from "@/infrastructure/sqlite/daily-command-center";
import {ProductNav} from "@/app/product-nav";
import {CandidateRefreshControl} from "./refresh-control";

export const dynamic="force-dynamic";
const label=(value:string)=>value.replaceAll("-"," ");
export default async function CandidatesPage({searchParams}:{searchParams:Promise<{track?:string;status?:string;added?:string;professional?:string;recruiter?:string;companies?:string;credits?:string}>}){const data=loadDailyCommandCenter(),params=await searchParams,track=params.track??"all",status=params.status??"all",rows=data.reserve.filter((row)=>(track==="all"||row.track===track)&&(status==="all"||row.stage===status)),showResult=params.added!==undefined;return <main><ProductNav current="candidates"/><header className="product-header"><div><h1>Candidates</h1><p>Your reserve of qualified and blocked contacts. Refresh uses the reserve first.</p></div><CandidateRefreshControl available={data.pipeline.available}/></header>{showResult?<section className="product-section"><p><strong>{params.added} new candidates added</strong><br/>{params.professional} professional · {params.recruiter} recruiter · {params.companies} companies · {params.credits} Apollo credits used</p></section>:null}<section className="product-section"><form className="simple-filters"><label>Track<select name="track" defaultValue={track}><option value="all">All</option><option value="professional">Professional</option><option value="recruiter">Recruiter</option></select></label><label>Status<select name="status" defaultValue={status}><option value="all">All</option><option value="qualified-available">Available</option><option value="cooldown">Cooldown</option><option value="suppressed">Suppressed</option><option value="already-contacted">Contacted</option><option value="enriched-unqualified">Blocked</option></select></label><button>Filter</button></form><div className="table-wrap"><table><thead><tr><th>Recipient</th><th>Company</th><th>Title</th><th>Track</th><th>Fit</th><th>Status</th></tr></thead><tbody>{rows.map((row)=><tr key={row.id}><td>{row.recipient}</td><td>{row.company}</td><td>{row.title}<small>{label(row.functionName)} · {row.industry}</small></td><td>{row.track??"professional"}</td><td>{row.score}</td><td>{label(row.stage)}</td></tr>)}</tbody></table></div></section></main>}
