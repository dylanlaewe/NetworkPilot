import {describe,expect,it} from "vitest";import {readFileSync} from "node:fs";import {join} from "node:path";
describe("candidate review safety UI",()=>{it("states fixture boundaries and exposes review actions without delivery controls",()=>{const source=readFileSync(join(process.cwd(),"src/app/candidate-review/page.tsx"),"utf8");expect(source).toContain("No provider is connected");expect(source).toContain("No delivery capability exists");expect(source).toContain("Fixture data only");expect(source).toContain("source-reported employer");for(const action of ["confirm","correct","reject","suppress","pending"])expect(source).toContain(`\"${action}\"`);expect(source).not.toMatch(/>Send</);});});
