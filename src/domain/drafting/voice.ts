export const DYLAN_VOICE_VERSION="dylan-outreach-method-v3" as const;
export const BANNED_OUTREACH_PHRASES=["i hope this message finds you well","i hope this email finds you well","i am reaching out to","i would love the opportunity","your impressive background","impressive background","your work really stood out","pick your brain","leverage","synergy","excited to connect","relevant introduction","networking opportunity"] as const;
export interface OutreachQuality {wordCount:number;ctaCount:number;firstPersonOpenings:number;warnings:string[];}
export function inspectOutreachQuality(body:string):OutreachQuality{const paragraphs=body.split(/\n\s*\n/).map((value)=>value.trim()).filter(Boolean),sentences=body.split(/(?<=[.!?])\s+/).map((value)=>value.trim()),wordCount=body.trim().split(/\s+/).filter(Boolean).length,ctaCount=(body.match(/\?/g)??[]).length,firstPersonOpenings=sentences.filter((sentence)=>/^(I|I’m|I've|My)\b/i.test(sentence)).length,warnings=[...(wordCount>100?["above-preferred-word-target"]:[]),...(sentences.some((sentence)=>sentence.split(/\s+/).length>28)?["long-sentence"]:[]),...(firstPersonOpenings>3?["excessive-first-person-openings"]:[]),...(new Set(paragraphs.map((value)=>value.toLowerCase())).size!==paragraphs.length?["repeated-idea"]:[])];return{wordCount,ctaCount,firstPersonOpenings,warnings};}
export function assertDylanVoice(subject:string,body:string){const text=`${subject}\n${body}`,lower=text.toLowerCase(),trimmedSubject=subject.trim();if(text.includes("—"))throw new Error("dylan-voice-em-dash-prohibited");if(BANNED_OUTREACH_PHRASES.some((phrase)=>lower.includes(phrase)))throw new Error("dylan-voice-generic-phrase-prohibited");if(!trimmedSubject||trimmedSubject.length>64||/[!?]$/.test(trimmedSubject)||/\b(connecting|opportunity)\b/i.test(trimmedSubject)||/[\p{Extended_Pictographic}]/u.test(trimmedSubject))throw new Error("dylan-voice-subject-invalid");const quality=inspectOutreachQuality(body);if(quality.wordCount<65||quality.wordCount>120)throw new Error("dylan-voice-word-count-invalid");if(quality.ctaCount!==1)throw new Error("dylan-voice-cta-count-invalid");}
