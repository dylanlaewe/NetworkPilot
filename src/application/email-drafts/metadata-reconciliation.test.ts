import {describe,expect,it} from "vitest";
import {gmailMetadataReconciliationEnabled,matchOwnedSentMessage} from "./metadata-reconciliation";
const owned={snapshotId:"snapshot",gmailDraftId:"draft",gmailMessageId:"message",rfcMessageId:"<owned@example.invalid>"};
describe("optional Gmail metadata sent detection",()=>{it("is disabled by default",()=>expect(gmailMetadataReconciliationEnabled({})).toBe(false));it("matches only one exact NetworkPilot-owned sent identity",()=>expect(matchOwnedSentMessage(owned,[{gmailMessageId:"message",rfcMessageId:null,labelIds:["SENT"],sentAt:"2026-09-17T12:00:00Z"}])).toEqual({kind:"exact",snapshotId:"snapshot",sentAt:"2026-09-17T12:00:00Z"}));it("does nothing for non-sent or unrelated metadata",()=>expect(matchOwnedSentMessage(owned,[{gmailMessageId:"other",rfcMessageId:null,labelIds:["INBOX"],sentAt:"2026-09-17T12:00:00Z"}])).toEqual({kind:"none"}));it("does not guess when a provider result is ambiguous",()=>{const row={gmailMessageId:"message",rfcMessageId:null,labelIds:["SENT"],sentAt:"2026-09-17T12:00:00Z"};expect(matchOwnedSentMessage(owned,[row,row])).toEqual({kind:"ambiguous"});});});
